fix(audit): address audit findings F-1, C-3, P0-2 and lint cleanup

Applied fixes from the 2026-08-23 multi-agent audit report (P0 + P1):

- F-1 (CRITICAL): theme-ipc.ts imported MAX_THEME_PACKAGE_BYTES from
  src/engine/src/theme/package.mjs (C4 boundary violation). Now imports
  the main-layer constant from ../theme/utils.
- C-3 (CRITICAL): CLEAR_ADAPTERS_BODY only cleared marker.interval; the
  adapters also stash sheetGuardInterval (and doubao sheetPoll) which
  survived restore and kept re-injecting sheets. Both are now cleared.
- P0-2: removed the blanket [class*="input-box"] * universal descendant
  rule in doubaoCss.mjs (hit every inner div, 217+ elements).
- P0-4: removed 16 root-level pollution files (bio-*.txt, vit-*.txt,
  tsc-out.txt, .tsbuildinfo) produced by parallel tool runs.
- Lint cleanup: dropped unused imports (AppRunState, SPACING_BASE,
  SPACING_MULTIPLIERS, wcagCheck, hexToOklch/oklchToHex, ColorTokenKey),
  replaced `as any` with `as unknown as ThemeState` in Studio test mocks,
  wrapped preview semicolons in {';'} (JSX lint), exported ThemeState,
  and re-sorted theme-ipc imports via biome.

Verified: npm run check green (3429 tests, 10 themes contract/staleness/
WCAG, injection contract, license headers, architecture, design tokens).
