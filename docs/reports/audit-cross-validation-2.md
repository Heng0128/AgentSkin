# 交叉校验报告：18 条质询 × 6 份审计草稿覆盖度评估

> **校验人**：AgentSkin 交叉校验专家（覆盖率检查）  
> **日期**：2026-08-19  
> **输入**：audit-draft-A 至 audit-draft-F（共 6 份）  
> **方法**：逐条比对质询清单与 6 份草稿内容，评估覆盖状态与回答深度  
> **约束**：仅做覆盖度评估，不修改任何审计草稿内容

---

## 一、逐质询覆盖状态总表

| # | 质询 | 主覆盖稿 | 覆盖状态 | 回答深度 | 逻辑闭环 |
|---|------|---------|---------|---------|---------|
| 1 | 探针执行时机 (a)(b)(c) | A | 已覆盖 | 深度分析 | 完整 |
| 2 | 探测不全处理 (a)(b) | A | 已覆盖 | 深度分析 | 完整 |
| 3 | 多Agent差异化时序隔离 | A | 已覆盖 | 深度分析 | 完整 |
| 4 | 脚本完整性校验 (a)(b) | B | 已覆盖 | 深度分析 | 完整 |
| 5 | DOM与样式扫描完整性 (a)(b) | B | 已覆盖 | 深度分析 | 完整 |
| 6 | 扫描后过滤 (a)(b)(c) | B | 已覆盖 | 深度分析 | 完整 |
| 7 | 基线存储 (a)(b) | C | 已覆盖 | 深度分析 | 完整 |
| 8 | 签名比对逻辑 (a)(b) | C | 已覆盖 | 深度分析 | 完整 |
| 9 | Shadow-Root边界 (a)(b) | C | 已覆盖 | 深度分析 | 完整 |
| 10 | AGENT_REMAP集成 (a)(b) | D | 已覆盖 | 深度分析 | 完整 |
| 11 | adapter.verification契约 (a)(b) | D | 已覆盖 | 深度分析 | 完整 |
| 12 | verify-style与局部覆写 (a)(b) | D | 已覆盖 | 深度分析 | 完整 |
| 13 | extract/manifest/adapter闭环 | D | 已覆盖 | 深度分析 | 完整 |
| 14 | 注入规约 (a)(b)(c) | E | 已覆盖 | 深度分析 | 完整 |
| 15 | 宿主动态变更 (a)(b) | E | 已覆盖 | 深度分析 | 部分缺口 |
| 16 | 回归链路 (a)(b) | F | 已覆盖 | 深度分析 | 完整 |
| 17 | 优先级定义 (a)(b) | F | 已覆盖 | 深度分析 | 部分缺口 |
| 18 | 数据流污染 (a)(b) | F | 已覆盖 | 深度分析 | 完整 |

**覆盖率统计**：18/18 条质询均有至少一份草稿主覆盖，无完全缺失项。

---

## 二、逐质询详细评估

### Q1. 探针执行时机：(a) 就绪判定逻辑 (b) 过早探测处理 (c) 重探测触发

**主覆盖稿**：A（§1.1-1.3）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 就绪判定逻辑 | **深度分析**。A 稿对比了三条链路的就绪判定方式：cdp-full-extract 为纯定时器（600ms sleep），injector.mjs 有双预算 waitForCompatibility，analyze-structure-compare 无等待逻辑。代码证据充分（L32、L676-677、L220-242）。 |
| (b) 过早探测处理 | **深度分析**。A 稿 F1 明确指出"高初始化 Agent 可能在 600ms 内未完成渲染，导致捕获残缺 DOM"，并给出风险矩阵。 |
| (c) 重探测触发 | **部分覆盖**。A 稿分析了 injector 的双预算轮询重试（L277-282）和 cdp-full-extract 的"无重试"现状，但**未深入分析"什么条件下应触发重探测"的判定逻辑**——即：超时后重试 vs 直接降级的决策边界。方案推演中提出了并行采集但未展开重探测触发条件的设计。 |

**逻辑闭环**：基本完整，但 (c) 的"触发条件"维度可补充。

---

### Q2. 探测不全处理：(a) 天然残缺场景 (b) 残缺数据连锁阻断

**主覆盖稿**：A（§2）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 天然残缺场景 | **深度分析**。A 稿列出 10 个天然残缺场景（S1-S10），覆盖懒加载、closed Shadow-Root、CORS 样式表、adoptedStyleSheets 等，每个场景标注代码证据和当前处理方式。 |
| (b) 残缺数据连锁阻断 | **深度分析**。A 稿 §2.2 的"探测结果残缺后的行为"表覆盖了 7 种行为模式，包括"忽略残缺直接输出""记录 error 但继续解析""残缺快照当基线持久化无防护"等。§2.3 进一步指出最高风险缺口：残缺数据无质量标记。 |

**逻辑闭环**：完整。

---

### Q3. 多Agent差异化：时序配置隔离

**主覆盖稿**：A（§3）

| 子项 | 覆盖评估 |
|------|---------|
| 时序配置隔离 | **深度分析**。A 稿 §3.1 完整列出了 cdp-full-extract 和 injector 的全局统一时序常量表，§3.2 给出了 6 维隔离分析矩阵（端口/串行/超时/错误/状态/SessionPool），§3.3 得出结论：不存在显式机制，依赖隐式隔离。方案推演提出 AGENT_TIMING per-Agent 配置表 + MAX_CONCURRENCY 并行采集。 |

**逻辑闭环**：完整。

---

### Q4. 脚本完整性校验：(a) CSP/JS污染识别 (b) 版本管理

**主覆盖稿**：B（§4）

| 子项 | 覆盖评估 |
|------|---------|
| (a) CSP/JS污染识别 | **深度分析**。B 稿 §4(a)-2 明确回答"当前能力：无法识别"CSP 拦截，§4(a)-3 列出 7 种被污染 API 的识别差距矩阵（querySelectorAll、getComputedStyle、shadowRoot 等），仅 CSS.escape 有 fallback。 |
| (b) 版本管理 | **深度分析**。B 稿 §4(b)-1 分析了统一代码基座 + adapter 参数化分发模型，§4(b)-2 列出 6 维版本载体状态表，§4(b)-3 给出 5 种版本不匹配场景的检测缺口。 |

**逻辑闭环**：完整。

---

### Q5. DOM与样式扫描完整性：(a) 覆盖范围 (b) 覆盖率指标

**主覆盖稿**：B（§5）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 覆盖范围 | **深度分析**。B 稿 §5(a)-1 给出 11 种样式来源 × 4 条链路的覆盖矩阵，标注了每个来源在各链路中的覆盖状态。§5(a)-2 深入分析了 4 个核心盲区（adoptedSheets、CSS-in-JS、closed Shadow DOM、!important）。 |
| (b) 覆盖率指标 | **浅层提及**。B 稿 §5(b) 仅指出"现状：未引入"，列举了缺失的指标字段（CORS 阻塞占比、adoptedSheets 支持状态等），但未深入分析**如何定义覆盖率分母**（"全部有效样式"本身是一个动态集合）。方案推演提出了 coverage 字段设计但未展开分母定义问题。 |

**逻辑闭环**：部分缺口——覆盖率指标的分母定义（什么是"应扫描的全体"）未解决。

---

### Q6. 扫描后过滤：(a) 业务Token vs 噪声 (b) 噪声混入防护 (c) 多Agent过滤规则

**主覆盖稿**：B（§6）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 业务Token vs 噪声 | **深度分析**。B 稿 §6(a)-1 完整分析了 categorizeVars 的 9 组正则模式及其 5 个问题（单维度命名启发式、正则重叠冲突、全局一套规则等）。 |
| (b) 噪声混入防护 | **深度分析**。B 稿 §6(b) 分析了 4 条噪声进入路径和 valueForToken 的 fallback 到 bg 的无差别映射问题。 |
| (c) 多Agent过滤规则 | **深度分析**。B 稿 §6(c) 给出 10 个模块的 per-agent 差异分析表，结论为"全局一套 + engine 侧有 per-agent 差异"。 |

**逻辑闭环**：完整。

---

### Q7. 基线存储：(a) 持久化位置 (b) 版本变更触发

**主覆盖稿**：C（§7）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 持久化位置 | **深度分析**。C 稿从 4 个证据源（semantic-snapshot.mjs 定位声明、buildSemanticSnapshot 纯函数、validateSnapshotCompatibility 仅做 schema 校验、RFC §4.7 禁止持久化）推导出"无持久化基线存储"的结论，并给出 3 种方案推演。 |
| (b) 版本变更触发 | **深度分析**。C 稿 §7(b) 指出快照中无 appVersion 字段、validateSnapshotCompatibility 不感知 Agent 版本，给出 5 种触发条件的推荐矩阵（appVersion 变化→触发，引擎版本变化→不触发等）。 |

**逻辑闭环**：完整。

---

### Q8. 签名比对逻辑：(a) 严格相等 vs 相似度 (b) 阈值体系

**主覆盖稿**：C（§8）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 严格相等 vs 相似度 | **深度分析**。C 稿 §8(a) 完整还原了判定链路：colorDistance（连续 0..1）→ matchesToken（二值化）→ assessStyleCompliance（OR 聚合）→ aggregateByRegion（riskLevel 分通道），并指出 3 个设计问题（连续信息丢弃、OR 过于宽松、minRatio=1 过严）。 |
| (b) 阈值体系 | **深度分析**。C 稿 §8(b) 指出全局统一固定阈值（tolerance=0.08, minRatio=1），无逐 Agent/组件差异化，给出 6 Agent 差异化阈值缺口表和 4 种方案推演（含分阶段落地建议）。 |

**逻辑闭环**：完整。

---

### Q9. Shadow-Root边界：(a) 不可校验标记 (b) 误统计风险

**主覆盖稿**：C（§9）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 不可校验标记 | **深度分析**。C 稿 §9(a) 从 dom-snapshot.mjs 的 openShadowRoots 计数、querySelectorAll 不穿透、输出报告无 closed shadow 字段三个层面推导出"完全不可见、不可计数、不可校验"，并给出 4 个推荐标记字段。 |
| (b) 误统计风险 | **深度分析**。C 稿 §9(b) 发现"空采样通过"逻辑漏洞（judged=0 → matchRatio=1 → pass=true），提出三态判定模型（PASS/FAIL/UNVERIFIABLE）。 |

**逻辑闭环**：完整。

---

### Q10. AGENT_REMAP集成：(a) 能力边界 (b) 跨Agent隔离

**主覆盖稿**：D（§10）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 能力边界 | **深度分析**。D 稿 §10(a) 指出脚本缺乏明确能力边界声明，AGENT_REMAP 完全硬编码，valueForToken 的启发式规则无法处理语义反转。给出契约缺口 G10.1 和 3 种方案推演。 |
| (b) 跨Agent隔离 | **深度分析**。D 稿 §10(b) 指出 cdp-full-extract 有物理隔离但 build-theme-package 缺乏语义隔离，extract ↔ builder 之间无数据流管道。给出契约缺口 G10.2。 |

**逻辑闭环**：完整。

---

### Q11. adapter.verification契约：(a) 阻断 vs 告警 (b) 自定义组件扩展

**主覆盖稿**：D（§11）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 阻断 vs 告警 | **深度分析**。D 稿 §11(a) 明确回答：check-semantic-contract 的 fail() 会 process.exit(1) 阻断 npm run check，但组件名未登记仅影响 verify-style 的风险分级（回退 MEDIUM），不直接阻断。给出契约缺口 G11.1。 |
| (b) 自定义组件扩展 | **深度分析**。D 稿 §11(b) 指出 COMPONENT_INDEX 是全局固定集合（Object.freeze），不支持 per-agent 扩展。registry 允许每 Agent 独有组件名但必须回退到 COMPONENT_INDEX 全局定义。给出契约缺口 G11.2。 |

**逻辑闭环**：完整。

---

### Q12. verify-style与局部覆写：(a) 扁平结构信息丢失 (b) 全局 vs 局部差异区分

**主覆盖稿**：D（§12）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 扁平结构信息丢失 | **深度分析**。D 稿 §12(a) 从 assessStyleCompliance 的签名（{key, color, bg, border} 扁平结构）出发，指出完全丢失组件局部 token 覆写信息。给出契约缺口 G12.1 和实际案例。 |
| (b) 全局 vs 局部差异区分 | **深度分析**。D 稿 §12(b) 明确回答"当前完全无法区分"，miss 输出无分类维度，nonControlled 信息停留在 selectivity-registry 不向 verify-style 传达。给出契约缺口 G12.2。 |

**逻辑闭环**：完整。

---

### Q13. extract/manifest/adapter闭环：跨Agent数据错配

**主覆盖稿**：D（§13）

| 子项 | 覆盖评估 |
|------|---------|
| 跨Agent数据错配 | **深度分析**。D 稿 §13(a) 从 4 个关键断点（build-theme-package 不校验 agentId 合法性、不读取 adapter.verification、VERIFICATION 与 adapter.verification 不一致、extract 不与 AGENT_REMAP 做 diff）推导出"缺乏严格一一对应保证"。给出 5 行跨 Agent 数据错配风险矩阵和契约缺口 G13.1。 |

**逻辑闭环**：完整。

---

### Q14. 注入规约：(a) 允许/跳过清单 (b) 条件规则 (c) verify-style区分跳过vs失败

**主覆盖稿**：E（§14）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 允许/跳过清单 | **深度分析**。E 稿 §14(a) 从 selectivity-registry、semantic-filter、renderer-payload 三处反推实际注入/跳过决策点，给出 9 类容器的"当前状态 vs 应有规约"对比表，以及 6 Agent 语义控制配置缺口表。 |
| (b) 条件规则 | **深度分析**。E 稿 §14(b) 分析了三种处理模式（Bridge 透明处理、nonControlled 过滤跳过、Renderer Profile 高级效果）的触发条件和缺失规约，汇总为 4 条隐式行为。 |
| (c) verify-style区分跳过vs失败 | **深度分析**。E 稿 §14(c) 明确指出 isNativeThemeControlled 默认返回 true 导致无 semantic 配置的 4 个 Agent 所有节点都参与判定，本应跳过的节点被纳入采样后自然不匹配，引擎无法区分"注入失败"与"应跳过"。给出 5 项每 Agent 单独调整能力的支持情况表。 |

**逻辑闭环**：完整。

---

### Q15. 宿主动态变更：(a) 重采流程 (b) 基线误报规避

**主覆盖稿**：E（§15）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 重采流程 | **浅层提及**。E 稿 §15(a) 正确识别了 4 条响应路径（MutationObserver、5s setInterval、CSS 变量桥接、verify-style 校验），指出"当前不存在宿主变更检测器、基线自动失效、语义签名重算、verify-style 误报抑制"。但**对"检测到宿主变更后如何触发重采"的具体流程设计未深入展开**——方案推演给出了 5 种方案但未选定推荐路径的详细设计。 |
| (b) 基线误报规避 | **深度分析**。E 稿 §15(b) 形式化了问题（B0 亮色基线 vs B1 暗色基线 vs T 主题 token vs S 采样结果），给出 5 种方案推演和短期/中期/长期推荐。 |

**逻辑闭环**：部分缺口——(a) 重采流程的详细设计（触发→重采→校验→恢复的完整状态机）未闭环。

---

### Q16. 回归链路：(a) 单Agent完整流程 (b) 批量隔离

**主覆盖稿**：F（§2）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 单Agent完整流程 | **深度分析**。F 稿 §2.1(a) 给出 6 步流程表（S1-S6），标注每步的代码位置和状态（✅/⚠️/❌），识别 5 个断点（BP-1 至 BP-5），包括语义基线采集入口缺失、复刻校验 Gate 无法离线运行、暗色/亮色失败静默接受等。 |
| (b) 批量隔离 | **深度分析**。F 稿 §2.2 对比了 analyze-structure-compare.mjs（隔离良好）和 cdp-full-extract.mjs（隔离缺口），给出 6 项批量隔离缺口清单（G1-G6）。 |

**逻辑闭环**：完整。

---

### Q17. 优先级定义：(a) P0分层 (b) kill-switch

**主覆盖稿**：F（§3）

| 子项 | 覆盖评估 |
|------|---------|
| (a) P0分层 | **深度分析**。F 稿 §3.1(a) 指出当前 P0/P1/P2 标度混淆了功能阻断型和质量型风险，提出 P0-Block / P0-Quality / P1 / P2 / P3 五级二维分级模型，给出每级的语义触发条件、工程语义和处置 SLA。 |
| (b) kill-switch | **部分覆盖**。F 稿 §3.1(b) 聚焦于"语义契约检测的 per-agent 开关"（AGENTSKIN_DISABLE_SEMANTIC_CHECK__zcode），设计了开关矩阵和审计日志要求。但**未覆盖更广义的 kill-switch 场景**：注入主流程的紧急停止（如发现主题注入导致 Agent 崩溃时的熔断机制）、verify-style 检测到严重漂移时的自动回滚触发等。 |

**逻辑闭环**：部分缺口——kill-switch 的覆盖范围偏窄，仅涉及诊断维度开关，未涉及运行时注入的紧急熔断。

---

### Q18. 数据流污染：(a) 阻断机制 (b) 主题包写入防护

**主覆盖稿**：F（§4）

| 子项 | 覆盖评估 |
|------|---------|
| (a) 阻断机制 | **深度分析**。F 稿 §4.1 分析了 4 个阻断点（A: totalNodes 无闸、B: stylesheets.error 无哨兵、C: truncated 标记无消费、D: baseline-validator 触发时机晚），给出每个阻断点的代码证据和缺口分析。 |
| (b) 主题包写入防护 | **深度分析**。F 稿 §4.2 追踪了从 cdp-full-extract → build-theme-package 的完整污染路径，指出 deriveTokens() 的 DEFAULT 回退掩蔽问题，给出 3 层防护设计（extract-validate → deriveTokens 严格模式 → build-agent-css 双料比对）。 |

**逻辑闭环**：完整。

---

## 三、跨稿交叉引用与一致性检查

### 3.1 一致发现（多稿印证）

| 发现 | 出现在 | 一致性 |
|------|--------|--------|
| cdp-full-extract 无质量标记 | A §2.3、B §4(a)、F §4.1 | 一致 |
| CORS 样式表变量静默丢失 | A §2.1 S5、B §4(a)-2、F §4.1 B | 一致 |
| adoptedStyleSheets 盲区 | A §2.1 S4、B §5(a)-2 盲区1 | 一致 |
| closed Shadow DOM 不可校验 | A §2.1 S3、B §5(a)-2 盲区3、C §9 | 一致 |
| verify-style 无法区分跳过vs失败 | D §12、E §14(c) | 一致 |
| 全局统一时序无 per-Agent 配置 | A §3.1、A §7 F7 | 一致 |
| DEFAULT_TOKENS 回退掩蔽 | D §10(a)、F §4.2(b) | 一致 |

### 3.2 潜在矛盾/口径差异

| 问题 | 稿 A 口径 | 稿 F 口径 | 评估 |
|------|----------|----------|------|
| cdp-full-extract 串行 vs 并行 | A §5.4 推荐并行 + MAX_CONCURRENCY | F §2.2.2 仅指出串行缺口，未推荐并行 | 非矛盾，A 更激进 |
| 残缺数据阻断优先级 | A §8.2 P0 建议增加 dataQuality 字段 | F §4.1 将 totalNodes 无闸标为 P0 | 非矛盾，两者均认为高优先级 |
| 基线存储方案 | C §7 推荐独立基线 JSON | E §15(b) 推荐双基线缓存 | 互补而非矛盾，C 关注持久化、E 关注运行时 |

---

## 四、需要补充的具体问题清单

### 4.1 深度不足需补充的质询（3 条）

| # | 质询 | 当前状态 | 需补充内容 | 建议补充稿 |
|---|------|---------|-----------|-----------|
| 5(b) | 覆盖率指标 | 浅层提及 | 覆盖率分母的定义方法：如何确定"应扫描的全体样式来源"？分母是静态枚举（已知 N 种样式来源类型）还是动态发现（运行时探测到的样式来源）？分母不确定会导致覆盖率指标无意义。 | B 稿扩展 |
| 15(a) | 重采流程 | 浅层提及 | 宿主变更→重采→校验→恢复的完整状态机设计：重采期间注入是否暂停？重采失败是否回退到旧基线？重采完成后 verify-style 的比对基准如何切换？ | E 稿扩展 |
| 17(b) | kill-switch | 部分覆盖 | 运行时注入的紧急熔断机制：当 verify-style 检测到 HIGH riskLevel 组件大面积失败时，是否自动回滚到上一个可用主题？kill-switch 的触发条件、回滚策略、恢复流程。 | F 稿扩展 |

### 4.2 逻辑闭环不足需补充的质询（1 条）

| # | 质询 | 缺口描述 | 建议 |
|---|------|---------|------|
| 1(c) | 重探测触发 | 仅分析了"有无重试"，未分析"什么条件下触发重探测"的判定逻辑：超时时长阈值、连续失败次数、降级后是否/何时重试。 | A 稿补充重探测触发条件矩阵 |

### 4.3 跨稿协同需明确的接口（2 条）

| # | 接口问题 | 涉及稿 | 建议 |
|---|---------|--------|------|
| I-1 | A 稿的 dataQuality 字段与 F 稿的 extract-validate 脚本是同一件事的不同方案，需统一为单一实施路径 | A + F | 明确：dataQuality 是元数据标记（轻量），extract-validate 是独立校验脚本（重量），两者可共存但需定义职责边界 |
| I-2 | C 稿的"基线持久化方案 A"与 E 稿的"双基线缓存方案 B"需对齐：持久化基线是否应同时缓存亮/暗两份？版本绑定粒度是否一致？ | C + E | 明确：持久化基线按 appVersion 绑定，运行时双基线按 themeMode 缓存，两者是不同层的机制 |

---

## 五、总结

### 5.1 覆盖度评价

- **18/18 条质询均有主覆盖**，无完全缺失项。这证明 6 份草稿的分工设计合理，覆盖了 CDP 探针→扫描→语义→集成→渲染→工程的全链路。
- **14/18 条达到深度分析**（有代码证据 + 风险等级 + 方案推演），质量优秀。
- **3/18 条为浅层提及或部分覆盖**：5(b) 覆盖率指标、15(a) 重采流程、17(b) kill-switch。
- **1/18 条有逻辑闭环缺口**：1(c) 重探测触发。

### 5.2 草稿质量评价

| 草稿 | 覆盖质询 | 核心优势 | 主要缺口 |
|------|---------|---------|---------|
| A | 1, 2, 3 | 三条链路对比分析，代码证据精确到行号 | 重探测触发条件未深入 |
| B | 4, 5, 6 | 脚本完整性识别差距矩阵，覆盖范围全景 | 覆盖率指标分母未定义 |
| C | 7, 8, 9 | 从"实验性定位"出发推导能力落差，逻辑严密 | 无明显缺口 |
| D | 10, 11, 12, 13 | 契约缺口编号体系（G10.1-G13.1），方案推演完整 | 无明显缺口 |
| E | 14, 15 | 6 Agent 差异化缺口矩阵，规约缺失清单 | 重采流程状态机未闭环 |
| F | 16, 17, 18 | 5 个断点识别，数据流污染路径追踪 | kill-switch 覆盖偏窄 |

### 5.3 建议后续行动

1. **补充 3 条深度不足的质询**（5(b)、15(a)、17(b)），由主覆盖稿各自扩展一个子章节。
2. **统一 2 个跨稿接口**（dataQuality vs extract-validate、持久化基线 vs 运行时双基线），避免后续实施方案冲突。
3. **逻辑闭环缺口**（1(c)）建议在 A 稿方案推演中增加"重探测触发条件矩阵"。
4. 完成上述补充后，6 份草稿可进入正式评审阶段。

---

*交叉校验完成。18 条质询全部有覆盖，14 条深度分析，3 条需补充深度，1 条需补充逻辑闭环。*
