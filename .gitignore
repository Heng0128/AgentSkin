node_modules/
.vite/
out/
coverage/
*.log
.DS_Store
.env
.env.*
*.p12
*.p8
.tsbuildinfo

# Standalone vite build output (verification runs)
dist/

# Build artifacts and debug residue
out_make_trash_*/
tmp-check*/
extracted-icons/
.build-tmp/
logs/
$null

# PowerShell backup / transient files
*.ps1.bak
find--result.txt
find-*-result.txt

# Tooling workspace caches (local-only)
.agnes/
.workbuddy/
.zcode/

# Windows reserved-name artifact (accidentally created by `> NUL` redirect)
/NUL

# CDP raw extraction dumps (large, regeneratable from live agents)
agents-raw-data/

# Probe screenshots (local-only capture artifacts)
assets/probe-shots/

# Consulting audit deliverables (not project source)
项目审计报告.md
战略审计报告.md
